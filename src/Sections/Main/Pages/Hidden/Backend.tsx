const Backend = (
    <div className="page-container">
        <h1>Backend</h1>
        <p>
            The backend endpoints use a mixture of web scraping and csv files to create endpoints. 
            The web scrapping uses a combination of BeautifulSoup and Selenium to create data. 
            The server uses python to convert csv files and create the endpoint data. 
        </p>
    </div>
)

export default Backend